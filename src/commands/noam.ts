import { ChatInputCommandInteraction } from "discord.js";

export default async function noam(interaction: ChatInputCommandInteraction) {
    await interaction.reply("<@YOUR_NOAM_ID> נועםםםםםםםםםםםםםםםם!");
}
