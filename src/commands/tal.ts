import type { ChatInputCommandInteraction } from "discord.js";

const tal = async (interaction: ChatInputCommandInteraction) => {
    await interaction.reply("<@1317796479511035956> טלללללללללללללללללללללל!");
};

export default tal;
